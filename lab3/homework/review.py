"""
1 . Why should we use functions at all?
 Their primary purpose is to help us organize programs 
into chunks that match how we think about the problem.

2 . How to define/declare a function?
     def NAME( PARAMETERS ): 
         STATEMENTS

3 . How to call/use a function?
after define function , you can call and use function 


4 . What is return, why and how do we use it?
The return statement is followed an expression (a in this case). 
This expression will be evaluated 
and returned to the caller as the “fruit” of calling this function.
vì có thể dùng biến cục bộ trong hàm vs nhiều mục đích khác nhau mà
sau mỗi hàm được định nghĩa xong thì biến sẽ bị xoá thì khi 
đó dùng return sẽ lôi được nó ra ngoài 


5 . Do we have to use return in every function?
lo tuỳ hàm ví dụ như void 
1 số hàm tự trả về NULL

6 . What are function arguments/parameters, why and how we use it?
PARAMETER :  name used inside a function to refer to the value 
which was passed to it as an argument.


7 . How to use function from a different file other than 
our currently working file?

khai báo nó đến từ 1 chỗ khác và import nó
"""